fn work(a, b: int): int {
    return (a + b * 3 + 11) % 1000
}

fn main() {
    n := 1000000
    mod := 100000
    total := 0
    i := 0

    for i < n {
        total = (total + work(i, total)) % mod
        i++
    }

    printf("%d\n", total)
}
